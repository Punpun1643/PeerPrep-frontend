import { Box, Typography } from "@mui/material";

function LogoutPage(props) {
    return (
        <Box style={{ margin: 'auto' }}>
            <Typography variant={"h1"}>You have been signed out successfully!</Typography>
        </Box>
    )
}

export default LogoutPage;